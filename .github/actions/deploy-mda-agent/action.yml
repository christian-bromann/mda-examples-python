name: Deploy MDA agent
description: Install deps with uv, write deploy .env from the environment, and run mda deploy.

inputs:
  agent:
    description: Agent project directory (e.g. policy-desk)
    required: true
  examples-path:
    description: Path to the examples repo checkout (relative to workspace)
    required: false
    default: mda-examples-python

runs:
  using: composite
  steps:
    - name: Validate agent directory
      shell: bash
      working-directory: ${{ inputs.examples-path }}
      run: |
        if [ ! -f "${{ inputs.agent }}/pyproject.toml" ] || [ ! -f "${{ inputs.agent }}/agent.py" ]; then
          echo "::error::'${{ inputs.agent }}' is not a deployable agent project"
          exit 1
        fi

    - name: Setup uv
      # v6+: python-version no longer auto-activates a workspace-root .venv
      # (v5 did, which made VIRTUAL_ENV point at the wrong path).
      uses: astral-sh/setup-uv@v6
      with:
        python-version: "3.12"

    - name: Install dependencies
      shell: bash
      working-directory: ${{ inputs.examples-path }}/${{ inputs.agent }}
      run: |
        uv sync
        # mda deploy shells out to `python -m pip install --target .mda-emit-site
        # ./__runtime__` for connector import resolution. uv venvs omit pip by default.
        uv pip install pip

    - name: Write deploy .env
      shell: bash
      working-directory: ${{ inputs.examples-path }}/${{ inputs.agent }}
      run: |
        if [ -z "$LANGSMITH_API_KEY" ]; then
          echo "::error::Missing repository secret LANGSMITH_API_KEY"
          exit 1
        fi
        if [ -z "$OPENAI_API_KEY" ]; then
          echo "::error::Missing repository secret OPENAI_API_KEY"
          exit 1
        fi

        umask 077
        : > .env
        write_env() {
          local key="$1"
          local value="$2"
          if [ -n "$value" ]; then
            printf '%s=%s\n' "$key" "$value" >> .env
          fi
        }

        write_env LANGSMITH_API_KEY "$LANGSMITH_API_KEY"
        write_env LANGSMITH_WORKSPACE_ID "$LANGSMITH_WORKSPACE_ID"
        write_env LANGSMITH_ENDPOINT "$LANGSMITH_ENDPOINT"
        write_env OPENAI_API_KEY "$OPENAI_API_KEY"
        write_env MDA_INGRESS_SECRET "$MDA_INGRESS_SECRET"
        write_env SLACK_BOT_TOKEN "$SLACK_BOT_TOKEN"
        write_env SLACK_SIGNING_SECRET "$SLACK_SIGNING_SECRET"
        write_env SLACK_USER_TOKEN "$SLACK_USER_TOKEN"
        # Agent tools expect GITHUB_TOKEN; Actions reserves that name for its own token.
        write_env GITHUB_TOKEN "$MDA_GITHUB_TOKEN"
        write_env LINEAR_API_KEY "$LINEAR_API_KEY"
        write_env X_BEARER_TOKEN "$X_BEARER_TOKEN"
        write_env MDA_GUEST_SIGNING_KEY "$MDA_GUEST_SIGNING_KEY"
        write_env VITE_SUPABASE_URL "$VITE_SUPABASE_URL"

        echo "Wrote .env with $(grep -c '=' .env) keys (values redacted)."

    - name: Deploy with mda
      shell: bash
      working-directory: ${{ inputs.examples-path }}/${{ inputs.agent }}
      env:
        AGENT: ${{ inputs.agent }}
      run: |
        echo "Deploying '$AGENT' as prod"
        uv run mda deploy . --deployment-type prod
